caso nao esteja iniciado: docker start android-container

docker exec -it android-container /bin/bash

------------------------------------------------------

Para construir e rodar a sua imagem Docker com suporte ao dispositivo KVM, você seguiria estes passos:

1. Alterar as permissões do arquivo start.sh no seu sistema host:

Abra um terminal no seu sistema host. Navegue até o diretório onde o arquivo start.sh está localizado.
Execute o comando para mudar as permissões do arquivo:

   chmod +x start.sh

2. Construir a Imagem:

   docker build -t minha-imagem-android .

3. Rodar o Contêiner:

   docker run -d -p 6080:6080 -p 9999:9999 -e EMULATOR_DEVICE="Samsung Galaxy S10" -e WEB_VNC=true --device /dev/kvm --name android-container minha-imagem-android


4. Se eu refiz a imagem docker, devo reconstruir assim:
	
	Salvar as alteracoes do Dockerfile e rodar o comando abaixo

   docker build -t minha-imagem-android .

Lembre-se, este comando de execução assume que você está rodando em um host que tem o dispositivo `/dev/kvm` disponível e configurado corretamente para uso. Isso é geralmente verdadeiro em sistemas Linux com suporte a virtualização habilitado. Se você estiver usando Docker em uma plataforma diferente ou se houver problemas com a configuração do KVM, esses passos podem precisar de ajustes.
